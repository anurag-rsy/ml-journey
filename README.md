# ML Journey

Daily Python/ML practice log — 5–7 AM, 6 days/week.

## Day 1 — June 30, 2025
**Topic:** Python functions + OOP

Built a `Particle` class (`particle.py`) demonstrating:
- Constructor (`__init__`) storing mass, velocity, charge as instance attributes
- `kinetic_energy()` method computing KE = 0.5 * m * v²
- Input validation with try/except in `main()`

**Resource used:** Automate the Boring Stuff, Ch. 3
